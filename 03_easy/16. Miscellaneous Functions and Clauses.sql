-- ============================================================
-- Interview Query — [Miscellaneous Functions and Clauses]
-- Section: [Easy]
-- Date: 2026-06-11
-- ============================================================


/* ============================================================
   ✅ WHAT IT IS
   ============================================================ */
-- The full execution order of SQL queries

1. FROM and JOIN
2. WHERE
3. GROUP BY
4. HAVING
5. ORDER BY
6. LIMIT

-- Other useful functions
-- ⭐️ IFNULL : make null followed by case
IFNULL([input],[output to return if input is NULL])

-- ⭐️ ROUND : round the input to a specific decimal place, which is specified in the function's second argument

-- ⭐️ CONCAT : combine two text columns as a single text column